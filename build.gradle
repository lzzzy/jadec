apply plugin: 'java'

repositories {
    jcenter()
}

test {
    testLogging.showStandardStreams = true
}

tasks.withType(Test) {
    enableAssertions = true
}

dependencies {
    testCompile 'junit:junit:4.12'
}
